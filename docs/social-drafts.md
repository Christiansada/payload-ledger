# Social drafts — not published

These drafts describe implemented local functionality. Publication is pending: the GitHub connection does not currently expose repository creation, and no verified live URL is available. Add actual links only after checking the published repository and application.

## LinkedIn

I built Payload Ledger, a small JSON fixture review tool for API developers and testers.

It compares parsed fields, matches records in one array by a unique ID, and lets you exclude explicit volatile paths. The ledger distinguishes additions, removals, type changes and value changes, with both source locations preserved when records move.

The interface runs locally in the browser and exports a JSON review. It includes synthetic examples, input validation, automated tests and accessibility checks. It is a review aid—not an API compatibility verdict—and uses JavaScript number precision.

The product is built locally; public source and hosting are pending. Useful next contributions include independent nested-data test oracles, screen-reader testing and exact decimal handling.

## X

Built Payload Ledger: a browser-local JSON fixture reviewer with ID-based array matching, explicit path exclusions and JSON review export. It separates value/type changes from reorder noise. Working locally; public repository and hosting pending. No compatibility verdicts.
