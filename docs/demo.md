# Demonstration plan (60–90 seconds)

Status: real working local app; automated desktop/mobile screenshots are generated by the browser suite. No video or public launch is claimed. Repository URL: unavailable pending creation through the connected GitHub plugin. Live URL: unavailable pending supported hosting and live verification. Add verified links before distributing a recording.

1. **Problem, 0–10s:** Explain that an API response can reorder records while changing only a few fields. Open the working app from `npm run preview` after building.
2. **Inputs, 10–25s:** Select **Load synthetic example**. Show the two invented parcel fixtures and the enabled `/records` → `id` matching rule. Explicitly say the data is synthetic.
3. **Useful output, 25–45s:** Show four changes: one type change, one value change, one added record and one removed record. Point out the two records at changed indices and the excluded generated timestamp.
4. **Interaction, 45–60s:** Filter to **Type changed**. Show the numeric-to-string weight change and the separate physical baseline/candidate pointers. Switch matching off and compare to demonstrate why array identity matters.
5. **Technical value, 60–75s:** Restore the sample and download the actual JSON report. Show that it includes all four changes even when filtered. Edit an input and show that export becomes unavailable until recomparison.
6. **Limits, 75–90s:** State that this is a local review aid, not a schema validator or compatibility verdict. Container additions are summaries; one array can be matched; decimal numbers use JavaScript precision. End with verified repository/live links only when they exist.

Do not substitute screenshots for an interactive demonstration or imply remote deployment from a local browser recording.
